angular.module 'simpleNotes.controllers'
  .controller 'PageController', ($scope, PagesService, $routeParams) ->
    PagesService.pages().then (pages) ->
      $scope.pages = pages
      $scope.page = _.find $scope.pages, (v) ->
        v.id == parseInt($routeParams.id)
